services:
  - type: web
    name: auto-anime-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m bot"
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /health
